PORT=5000
DB_HOST=localhost
DB_USER=libraryuser
DB_PASSWORD=your_db_password
DB_NAME=librarydb
DATABASE_URL="mysql://libraryuser:your_db_password@localhost:3306/librarydb"
JWT_SECRET="replace-with-a-long-random-string"

# Optional — reservation-ready emails no-op (just log) until this is set.
# Get a free key at https://resend.com
RESEND_API_KEY=
EMAIL_FROM="Bookshelf <onboarding@resend.dev>"
